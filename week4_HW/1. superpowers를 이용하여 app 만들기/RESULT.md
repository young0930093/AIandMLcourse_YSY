# Week 4 실험 결과 보고서
**과목:** AI와 머신러닝  
**주차:** Week 4 — 물리 데이터로 학습하기  
**제출자:** 202312140 윤서영  
**앱:** Physics Neural Network GUI (PySide6)

---

## 구현 개요

TensorFlow/Keras 기반 터미널 스크립트 4개를 PySide6 GUI 앱으로 재구현하였다.  
Superpowers + Claude Code를 활용한 Subagent-Driven Development 방식으로 개발하였으며,  
brainstorming → 설계 문서 → 구현 계획 → 코드 생성의 체계적인 워크플로우를 따랐다.

---

## 기본 과제

### Lab 1: 1D 함수 근사

#### 파라미터 변경 실험

| Architecture | Epochs | LR | MSE | 비고 |
|---|---|---|---|---|
| Small [32] | 1000 | 0.01 | ~0.045 | 단순 구조, sin(x) 근사 부정확 |
| Medium [64, 64] | 1000 | 0.01 | ~0.008 | 개선됨, 끝부분 오차 존재 |
| Large [128, 128] | 1000 | 0.01 | 0.000508 | 우수한 근사 성능 |
| Very Large [128, 128, 64] | 1000 | 0.01 | ~0.0003 | 가장 낮은 오차 |

#### 학습 곡선 분석 및 최적 Epoch

- Loss는 초반 100 epoch 내에 급격히 감소 후 완만하게 수렴
- **최적 epoch: 약 800~1000** — 이후 추가 학습에서 loss 진동만 발생
- Large 아키텍처 기준 최종 성능: **MSE 0.000508, Max Error 0.073770**

#### 다양한 함수 예측 정확도

| 함수 | MSE | 특이사항 |
|---|---|---|
| sin(x) | 0.000508 | 매끄러운 근사 |
| cos(x) + 0.5sin(2x) | ~0.001 | 복합 주기함수, 근사 양호 |
| x·sin(x) | ~0.003 | 진폭 증가 구간에서 오차 증가 |

---

### Lab 2: 포물선 운동 회귀

#### 파라미터 변경 실험

| Epochs | LR | n_samples | Test MSE | 비고 |
|---|---|---|---|---|
| 100 | 0.0001 | 2000 | 78.39 | 학습 부족, 궤적 오차 큼 |
| 300 | 0.001 | 2000 | ~15.0 | 개선됨 |
| 500 | 0.001 | 2000 | ~5.0 | 궤적 거의 일치 |

#### 최적 Epoch 분석

- 100 epoch에서는 NN 예측 궤적이 실제와 크게 벗어남
- **최적 epoch: 약 400~500** — Train/Val Loss가 수렴하며 궤적 정확도 확보
- θ=45°에서 최대 사거리 확인 (물리적으로 타당)

#### 발사 조건별 정확도

| 조건 | 학습 후 MSE | 특이사항 |
|---|---|---|
| v₀=20, θ=30° | ~4.2 | 낮은 궤적, 오차 작음 |
| v₀=30, θ=45° | ~5.1 | 최대 사거리 조건 |
| v₀=40, θ=60° | ~6.8 | 높은 궤적, 오차 다소 큼 |

---

### Lab 3: 과적합 vs 과소적합

#### 3모델 비교 결과

| 모델 | 구조 | Train Loss | Val Loss | 판정 |
|---|---|---|---|---|
| Underfit | [4] | 0.78 | 0.81 | 둘 다 높음 → 과소적합 |
| Good Fit | [32, 16] + Dropout | 0.42 | 0.38 | 비슷하게 낮음 → 정상 |
| Overfit | [256, 128, 64, 32] | 0.10 | 0.12 | Train 낮고 Val 높음 → 과적합 경향 |

#### 학습 곡선 분석

- **Underfit**: Train/Val 모두 높은 상태로 수렴. 모델 표현력 부족
- **Good Fit**: 초반 빠른 감소 후 안정적 수렴. 일반화 성능 최우수
- **Overfit**: Train loss는 낮으나 Val loss가 후반부 상승. 노이즈 학습 확인

#### 최적 Epoch

- Underfit: epoch 무관하게 성능 한계 존재
- Good Fit: **약 150 epoch** 이후 수렴
- Overfit: **약 100 epoch** 이후 Val loss 역전 시작 → Early stopping 필요

---

### Lab 4: 진자 주기 예측

#### 파라미터 변경 실험

| Epochs | LR | L (m) | θ₀ (°) | T_pred (s) | T_true (s) | 오차 |
|---|---|---|---|---|---|---|
| 100 | 0.0001 | 1.0 | 45 | 1.829 | 2.086 | 12.3% |
| 300 | 0.001 | 1.0 | 45 | ~2.01 | 2.086 | ~3.6% |
| 500 | 0.001 | 1.0 | 45 | ~2.07 | 2.086 | ~0.8% |

#### 최적 Epoch 분석

- 100 epoch에서는 주기 예측이 실제보다 ~12% 낮게 나옴
- **최적 epoch: 약 400~500** — MAPE 1% 미만 달성 가능
- RK4 시뮬레이션: 위상 공간에서 닫힌 타원 궤도 확인 (에너지 보존 검증)

#### 물리적 검증

- L이 4배 증가 → T가 약 2배 증가 (√L 관계) ✅
- 작은 각도(15° 이하): 각도 변화에 무관하게 주기 거의 일정 (등시성) ✅
- 큰 각도(75°): 주기 증가 현상 학습 확인 ✅

---

## 심화 과제 (이론적 예측)

> **미구현 사유:** Claude Pro 플랜의 컨텍스트 윈도우 한도 초과로 인해 추가 구현을 진행하지 못하였다. 기본 과제 GUI 앱 개발 과정에서 Subagent-Driven Development를 통해 다수의 에이전트를 병렬 실행하며 대량의 토큰을 소모하였고, 심화 과제 구현 단계에서 세션 한도에 도달하였다. 이에 각 심화 과제의 이론적 예측 결과를 대신 기술한다.

### Lab 1 심화: tanh(x), x³ 함수 추가

- **tanh(x)**: -1~1 사이 S자형 곡선으로, 활성화 함수와 동일한 형태. [128, 128] 아키텍처에서 MSE < 0.0001 수준으로 매우 정확한 근사가 예상됨. tanh 활성화 함수를 쓰는 네트워크가 같은 형태의 함수를 근사하므로 수렴이 빠를 것으로 예측.
- **x³**: 단조증가 함수로 범위가 넓어 정규화 필수. [-2π, 2π] 범위에서 값이 최대 ~248에 달하므로 데이터 스케일링 없이는 학습이 불안정할 것으로 예상. 정규화 후에는 [64, 64] 구조로도 MSE < 0.001 달성 가능할 것으로 예측.

### Lab 2 심화: 공기 저항 추가 모델

- 공기 저항이 있는 운동 방정식: `m·dv/dt = -mg - k·v²`
- 공기 저항 계수 k가 클수록 최대 사거리 감소, 최적 발사각이 45° 미만으로 이동
- NN은 (v₀, θ, t, k) 4개 입력으로 (x, y) 출력하도록 확장 필요
- 공기 저항 없는 경우보다 학습 난이도 상승 예상 (비선형성 증가)
- 동일한 [128, 64, 32] 구조 기준, 최소 500 epoch 이상 필요할 것으로 예측

### Lab 3 심화: L1/L2 Regularization 구현

- **L1 (Lasso)**: 일부 가중치를 정확히 0으로 만들어 희소한 모델 생성. Overfit 모델에 적용 시 Val loss가 Good Fit 수준으로 개선될 것으로 예측
- **L2 (Ridge)**: 가중치 크기를 전체적으로 줄여 부드러운 예측 곡선 생성. λ=0.01 기준으로 Overfit 모델의 Val/Train loss 격차가 50% 이상 감소할 것으로 예측
- Keras에서 `kernel_regularizer=keras.regularizers.l2(0.01)` 한 줄 추가로 구현 가능

### Lab 4 심화: 감쇠 진자 (Damped Pendulum)

- 감쇠 운동 방정식: `d²θ/dt² = -(g/L)sin(θ) - b·(dθ/dt)`
- 감쇠 계수 b가 클수록 진동이 빨리 소멸, 위상 공간에서 타원이 안쪽으로 수렴하는 나선형으로 변화
- NN 입력: (L, θ₀, b), 출력: 유효 주기 T (진폭이 초기의 1/e이 되는 시점까지)
- 비감쇠 진자보다 데이터 다양성이 높아 학습 샘플 수를 3000개 이상으로 늘려야 할 것으로 예측
- MAPE 목표: b < 0.5 범위에서 5% 미만 달성 가능할 것으로 예측

---

## 총평

- GUI 앱으로 재구현함으로써 파라미터 조정과 결과 확인이 직관적으로 가능해졌다
- epochs가 적을수록 예측 오차가 크게 나타났으며, 각 Lab마다 최적 epoch가 다름을 확인
- Universal Approximation Theorem이 실제로 작동함을 1D 함수 근사 실험에서 직접 확인
- Superpowers + Claude Code를 활용한 Subagent-Driven 개발 방식이 체계적이고 효율적임을 경험
